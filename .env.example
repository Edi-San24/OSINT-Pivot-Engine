# Copy to .env and fill in. .env is gitignored; never commit real keys.

# Required — the agent writes the investigation summary
ANTHROPIC_API_KEY=

# Core enrichment
VIRUSTOTAL_API_KEY=
SHODAN_API_KEY=
CENSYS_API_KEY=

# abuse.ch — one key covers MalwareBazaar, URLhaus and ThreatFox
THREATFOX_API_KEY=

# AlienVault OTX — community pulse enrichment
OTX_API_KEY=
